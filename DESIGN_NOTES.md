# 設計計算と描画の確認記録

## 中心距離の「和」と「差」

s＝太陽、p＝遊星、r＝リング。転位なし、共通モジュール m。

$$a_{SP}=\frac{m(z_s+z_p)}{2},\qquad a_{PR}=\frac{m(z_r-z_p)}{2}$$

外歯同士はピッチ半径の和、内歯との組合せは半径の差になる。
同心条件で両者を等しくすると $z_r=z_s+2z_p$。
この関係で遊星歯数を消去すれば、

$$a=\frac{m(z_r+z_s)}{4}$$

と表せる。したがって **リングと太陽を使う中心距離式の分子は和** で正しい。
旧実装は遊星との歯数和／差を使用しており、同心条件成立時は同じ中心距離だった。
条件が成立しない場合は、上の2つの中心距離は一致しないため、和による値だけで強制的に配置してはいけない。
同心条件の重複行は削除し、歯数関係だけを残した。

均等配置は別条件：$(z_s+z_r)/N\in\mathbb{Z}$。

## 描画できていた理由と修正

- 描画座標 `planetary_positions` の半径は $a_{SP}$。同心条件成立時には遊星とリングのピッチ円も正しく内接する。これはテストで座標から独立に照合した。
- 旧判定は歯数、均等配置、遊星間すきまのみ。ピッチ円が一致してもインボリュート干渉は回避できない。
- 旧プレビューの歯元は簡略接続で、切下げの創成形状を生成していない。また静止画像だけでは回転中の干渉も判断できない。
- 具体例：m=2、α=20°、太陽24・遊星18・リング60・遊星3個。中心距離はどの等価式でも42 mm、均等配置も成立するが、遊星–リングのインボリュート干渉回避条件に不適合。
- 新実装は解析式の判定を描画可否に接続し、この例では遊星を隠す。リングと太陽は参考形状として残す。形状自体は工具創成形状には変更していない。

## 入力範囲の意味

6歯以上などの上下限は初期アプリの実装上の制限。干渉のない歯数や製作可能性を示すものではない。
設計制約の一覧から削除し、入力欄の説明・ツールチップに移した。

## 内歯干渉の検査式

KHK「内歯車の寸法計算」式4.4〜4.12を使用。
以下で1＝遊星、2＝リング、$\operatorname{inv}\beta=\tan\beta-\beta$、角度はrad。

$$r_{a1}=m(z_p/2+1),\quad r_{a2}=m(z_r/2-1),\quad
r_{bi}=mz_i\cos\alpha/2$$
$$a=m(z_r-z_p)/2,\quad \alpha_w=\alpha,\quad
\alpha_{ai}=\arccos(r_{bi}/r_{ai})$$

各ペアの標準中心距離で計算する。同心条件不成立時の強制かみ合いには適用しない。
リング歯先円が基礎円以下、非正の中心距離、逆三角関数の定義域外は「判定不可」。丸め誤差を超える値をクリップして合格扱いにはしない。

### インボリュート干渉（運転時）

$$\frac{z_p}{z_r}\ge 1-\frac{\tan\alpha_{a2}}{\tan\alpha}$$

### トロコイド干渉（運転時）

$$\theta_1\frac{z_p}{z_r}+\operatorname{inv}\alpha-\operatorname{inv}\alpha_{a2}\ge\theta_2$$
$$\theta_1=\arccos\frac{r_{a2}^2-r_{a1}^2-a^2}{2ar_{a1}}
+\operatorname{inv}\alpha_{a1}-\operatorname{inv}\alpha$$
$$\theta_2=\arccos\frac{a^2+r_{a2}^2-r_{a1}^2}{2ar_{a2}}$$

英語版の式4.8画像は加算記号が欠けていたため、日本語公式版と照合して実装した。

### トリミング干渉（逃げ干渉、組立時）

$$\theta_1+\operatorname{inv}\alpha_{a1}-\operatorname{inv}\alpha
\ge\frac{z_r}{z_p}(\theta_2+\operatorname{inv}\alpha_{a2}-\operatorname{inv}\alpha)$$

この節のθはトロコイド干渉のθとは別定義。

$$\theta_1=\arcsin\sqrt{\frac{1-(\cos\alpha_{a1}/\cos\alpha_{a2})^2}{1-(z_p/z_r)^2}}$$
$$\theta_2=\arcsin\sqrt{\frac{(\cos\alpha_{a2}/\cos\alpha_{a1})^2-1}{(z_r/z_p)^2-1}}$$

不適合は半径方向の組立・取り外しに制約があるという意味。軸方向組立を検討する注意として表示し、運転中の干渉と混同して遊星を非表示にはしない。
この検査は歯車ペアに対するもので、ピニオンカッタの歯数・転位・歯末が未入力のため、工具の歯切り可否を保証しない。

### 切下げ（別現象）

標準ラック創成・歯末係数1・転位0の理論回避条件：

$$z\ge\frac{2}{\sin^2\alpha}$$

太陽と遊星に適用する。20°では理論値17.097歯なので「厳密に回避」は18歯以上と表示する。
実用上の17歯という目安や許容切下げとは区別する。切下げ量・実際の工具先端丸み・残存歯厚は未計算。
切下げがあっても即運転不可とは限らないため加工上の注意とし、表示歯形には切下げ形状を反映しない。

## 検証と適用範囲

KHK標準内歯車カタログの60・80・100・120・160・200歯に対し、
インボリュート干渉の許容下限、トロコイド／トリミング干渉の許容上限とその直前・直後を照合。36ケース一致。
加えて圧力角の変更、モジュールに対する判定不変性、式の定義域外、中心距離と描画座標の整合を検査。

転位、工具創成、切下げ後の歯元、太陽–遊星の詳細干渉、かみあい率、強度、バックラッシ、実際の組立経路全体は未評価。
リサイズのキャッシュは維持。300歯リングのオフスクリーン再計測は約9 ms/回。実操作の応答時間の保証ではない。

## 参照

- [KHK 遊星歯車機構](https://www.khkgears.co.jp/gear_technology/intermediate_guide/khk388/)
- [KHK 内歯車の寸法計算（日本語の原式）](https://www.khkgears.co.jp/gear_technology/basic_guide/khk362/)
- [KHK 内歯車カタログ・許容歯数表（208頁）](https://khkgears.net/pdf/internal-tech.pdf)
- [KHK FAQ・干渉の意味](https://www.khkgears.co.jp/gear_technology/faq/)
- [SDP/SI Elements of Metric Gear Technology・標準ラック切下げ条件](https://www.sdp-si.com/PDFS/Elements-of-Metric-Gear-Technology.pdf)
